package org.example;

public class Team(){
        private String Name;
        //should be array list check V2
        private Player [] Squad;
        public Team(String name){
            this.Name = name;
            this.Squad[i] = addPlayer(name, Squad));
        }

        //////NOT SUPPOSED TO BE STATIC
        public static Player[] addPlayer(Player name, Player [] Squad){
            Player [] addedSquad = new Player[Squad.length+1];
            for (int i = 0; i < Squad.length;i++) {
                addedSquad[i] = Squad[i];
            }
            addedSquad[Squad.length] = name;
            return addedSquad;
        }
        public static Player [] getAllPositionPlayers(Player Position){
            String [] PosPlayers; 
            for ( int i= 0; i < .length i++) {

                Position.ignoreCase();

            }
        }
        public static String findPlayerPositions(Player Name){
            {

        }return Position;

        }
        public static String [] getNames(Player name){
            String [] names = new String [Squad.length];
            for (int i = Squad.length; i++){]]]
                g

            }



        }


    }

